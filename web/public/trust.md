# Retained verification standard

The proposed storefront required five gates before a Base could list: build the workflow, run it in a real setup, cut unsupported scope, publish explicit evidence, and re-verify after upstream changes.

The standard rejected copied products, generic untested repositories, broken demos, hidden telemetry, unsupported AI output, and misleading comparison claims.
